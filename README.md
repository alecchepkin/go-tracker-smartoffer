An example of micro-service

The service manages a smartoffers in a tracker.

Can run from command line with -sub param or as a daemon.

If runs as daemon will launch subrotines in the background (like cron).